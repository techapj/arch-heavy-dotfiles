#!/usr/bin/env fish

if test -n $argv[1]
  set commit $argv[1]
else
  set commit HEAD
end

set then (git log --pretty=format:%at --max-count=1 "$commit")
set now (date +%s)

printf "%d\n" (math "$now - $then")
