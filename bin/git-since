#!/usr/bin/env zsh
set -ue

local commit

if [[ "$#" > 0 ]]; then
  commit="$1"
else
  commit="HEAD"
fi

local then_="$(git log --pretty=format:%at --max-count=1 "$commit")"
local now=$(date +%s)

printf "%d\n" "$(($now - $then_))"
